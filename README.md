# self-scaling
This component from the aeriOS Self-* is able to horizontally scale (up or down) the resources devoted to a specific service (inside a node) in a dynamic fashion, based on time series inference and custom logic
